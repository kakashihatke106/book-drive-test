<% if(error.length>0){%>
    <script>
        var error_messages = [];
            <% error.forEach(element => {%>
            error_messages.push('<%= element%>');
            <%});%>
            alert(error_messages.join('\n'));
    </script>
    <%}%>